% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ramsar_indicator.R
\name{calc_ramsar_indicator}
\alias{calc_ramsar_indicator}
\title{Batch Calculate and Save Ramsar Biodiversity Indicators}
\usage{
calc_ramsar_indicator(
  indicator,
  inputdir,
  maindir,
  shapefiledir,
  continent,
  plot_args = list(),
  ...
)
}
\arguments{
\item{indicator}{Character. The indicator to calculate, combined with type
(e.g., "obs_richness_ts", "total_occ_map").}

\item{inputdir}{Character. Path to the base directory containing country-level
subdirectories with GBIF data cubes.}

\item{maindir}{Character. Path to the base output directory.}

\item{shapefiledir}{Character. Path to the base directory containing site WKT files.}

\item{continent}{Character. The continent name used for b3gbi internal mapping.}

\item{plot_args}{List. Additional arguments passed to the plot function
(e.g., \code{list(smoothed_trend = FALSE)}).}

\item{...}{Additional arguments passed to \code{b3gbi::compute_indicator_workflow}.}
}
\value{
A list containing two nested lists: \code{mean} (mean indicator values
  per site) and \code{values} (raw indicator values per site).
}
\description{
This function iterates through Ramsar sites in country subdirectories,
processes their GBIF data cubes, calculates a specific biodiversity
indicator using \code{b3gbi}, and saves the resulting plots.
}
